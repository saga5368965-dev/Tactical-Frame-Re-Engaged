{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "prepareRunClientCompile",
      "type": "shell",
      "command": "./gradlew prepareRunClientCompile",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}